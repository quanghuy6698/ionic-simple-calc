import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ironhuy.simpleCalc',
  appName: 'Simple Calc',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
